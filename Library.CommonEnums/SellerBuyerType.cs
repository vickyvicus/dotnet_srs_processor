namespace Libraries.CommonEnums
{
    public enum SellerType
    {
        CompanyAdmin = 10,
        SuperStockist = 20,
        Distributor = 30,
        SubDistributor = 40,
        Retailer = 50
    }
    public enum BuyerType
    {
        CompanyAdmin = 10,
        SuperStockist = 20,
        Distributor = 30,
        SubDistributor = 40,
        Retailer = 50
    }
}
