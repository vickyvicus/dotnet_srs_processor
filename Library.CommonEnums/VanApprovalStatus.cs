namespace Library.CommonEnums
{
    public enum VanApprovalStatus
    {
        Pending,
        Approved,
        Rejected
    }

    public enum VanSalesManagerApprovalStatus
    {
        Pending,
        Approved,
        Rejected
    }
}
